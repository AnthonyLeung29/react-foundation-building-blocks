import PropTypes from 'prop-types';

/**
 * A file that contains general prop types for common compononent proptypes.
 */

export const GeneralBtnPropTypes = {
  className: PropTypes.string,
  style: PropTypes.object
};
